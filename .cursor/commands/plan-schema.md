Actuá como Planner (.cursor/rules/planner.mdc). Basándote en este listado de endpoints:

$ARGUMENTS

Proponé el schema.prisma completo necesario para soportar todos los endpoints (auth, tareas, objetivos, kpis, stats). Incluí relaciones entre modelos y los campos mínimos que cada endpoint necesita. No implementes código todavía, solo el schema y un breve listado de qué tarea le corresponde a cada uno de los 4 builders (Auth, Tareas, Objetivos/KPIs, Stats), señalando dependencias.