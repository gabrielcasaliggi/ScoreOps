Implementar la siguiente feature usando el flujo Planner → Builder → Reviewer:

$ARGUMENTS

1. Activá el rol Planner (ver .cursor/rules/planner.mdc) y generá el plan de tareas
2. Mostrame el plan antes de continuar — esperá mi aprobación
3. Una vez aprobado, asigná cada tarea a un agente en su propio worktree
4. Al terminar cada tarea, activá el rol Reviewer sobre el resultado antes de aplicar el merge